import React from "react";
import { BasicTextFields, TextFieldProps, Validation } from "./";

export default {
  title: "Text Fields",
};

export const validation = () => <Validation />;

export const basicTextFields = () => <BasicTextFields />;

export const textFieldProps = () => <TextFieldProps />;
