import React from "react";
import { Checkboxes, CheckboxesGroup, CheckboxLabels } from "./";

export default {
  title: "MUI|Checkbox",
};

export const checkboxes = () => <Checkboxes />;

export const checkboxesGroup = () => <CheckboxesGroup />;

export const checkboxLabels = () => <CheckboxLabels />;
